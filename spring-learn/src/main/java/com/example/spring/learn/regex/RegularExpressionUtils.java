package com.example.spring.learn.util;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class RegularExpressionUtils {
    public static Matcher createMatcherWithTimeout(String s, Pattern pattern, int timeoutMillis) {
        CharSequence charSequence = new
    }

    private static class TimeoutReg
}
